# Misconfiguration IDs to skip — see https://trivy.dev/latest/docs/configuration/filtering/
# Prefer `#trivy:ignore:<ID>` on Terraform resources; use this file only when inline suppression is not possible.

# CI client image runs as root so Prow-mounted workspace is writable (not a deployable long-lived service).
DS-0002

# Go stdlib in CI client image (bundled ROSA/toolchain); fixed in Go 1.25.7+ — bump when upstream releases ship.
CVE-2025-68121
